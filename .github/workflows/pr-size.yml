# Managed by boxshop-cli (bs hooks) — do not edit
name: PR Size
on:
  pull_request:
    branches: [main]
    types: [opened, synchronize, reopened]
jobs:
  label:
    uses: boxshopio/boxshop-config/.github/workflows/pr-size.yml@main
    permissions:
      pull-requests: write
